;;; This is template. Copy to conf.lisp,
;;; it will be read during startup.
;;; Look at model/config.lisp for details

(:domain "example.com")
(:development nil)

(:other-thing
 (a b c d e))

(:you-got-the-point?
 (i think yes))
